export declare const StandardSupportedClouds: {
    readonly AWS: "AWS";
    readonly GCP: "GCP";
};
/**
 * The cloud that the standard is supported on.
 */
export declare type StandardSupportedClouds = (typeof StandardSupportedClouds)[keyof typeof StandardSupportedClouds];
